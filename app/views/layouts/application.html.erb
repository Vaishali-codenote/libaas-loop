<!DOCTYPE html>
<html>
  <head>
    <title>LibaasLoop - Ethnic Wear Rental Marketplace</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-white shadow-sm border-b">
      <div class="container mx-auto px-4">
        <div class="flex justify-between items-center h-16">
          <div class="flex items-center">
            <%= link_to "LibaasLoop", root_path, class: "text-xl font-bold text-indigo-600 tracking-tight" %>
            <div class="ml-8 flex items-center gap-2">
              <% if user_signed_in? %>
                <button type="button" onclick="window.history.length > 1 ? history.back() : window.location.assign('<%= root_path %>')" class="px-3 py-1.5 rounded-md text-gray-700 hover:bg-gray-100 transition">Back</button>
              <% end %>
              <% unless user_signed_in? %>
                <%= link_to "Browse", listings_path, class: "px-3 py-1.5 rounded-md text-gray-700 hover:bg-gray-100 transition" %>
              <% end %>
              <% if user_signed_in? %>
                <%= link_to "My Listings", my_listings_path, class: "px-3 py-1.5 rounded-md text-gray-700 hover:bg-gray-100 transition" %>
                <%= link_to "My Rentals", my_rentals_path, class: "px-3 py-1.5 rounded-md text-gray-700 hover:bg-gray-100 transition" %>
              <% end %>
            </div>
          </div>
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <span class="text-gray-600 text-sm">Hello, <%= current_user.name %></span>
              <% if current_user.admin? %>
                <%= link_to "Admin", admin_dashboard_path, class: "px-3 py-1.5 rounded-md bg-red-50 text-red-700 hover:bg-red-100 font-medium transition", data: { turbo: false } %>
              <% end %>
              <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "px-3 py-1.5 rounded-md text-gray-700 hover:bg-gray-100 transition" %>
            <% else %>
              <%= link_to "Login", new_user_session_path, class: "px-3 py-1.5 rounded-md text-gray-700 hover:bg-gray-100 transition" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "bg-indigo-600 text-white px-4 py-2 rounded-md hover:bg-indigo-700" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative container mx-auto mt-4" role="alert">
        <%= notice %>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative container mx-auto mt-4" role="alert">
        <%= alert %>
      </div>
    <% end %>

    <main class="container mx-auto mt-6 px-4">
      <%= yield %>
    </main>

    <%= render "shared/delete_modal" %>
  </body>
</html>
